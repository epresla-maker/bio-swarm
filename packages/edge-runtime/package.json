{
  "name": "@bio-swarm/edge-runtime",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'No linter configured for edge-runtime yet'",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@bio-swarm/shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
